# 人事承認待ち打刻修正一覧

## 概要

| 項目 | 内容 |
|------|------|
| 画面ID | SCR-CC-005 |
| 画面種別 | 一覧 |
| 関連集約 | 打刻修正申請（申請承認コンテキスト） |
| URL | `/clock-corrections/pending-hr` |

---

## ワイヤーフレーム

```
┌───────────────────────────────────────────────────────────────────────────┐
│ {システム名}                                                  [user ▼]   │
├───────────────────────────────────────────────────────────────────────────┤
│ パンくず: ホーム > 申請管理 > 人事承認待ち打刻修正一覧                   │
├───────────────────────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────────────────────────┐ │
│ │ 検索条件                                                             │ │
│ │                                                                     │ │
│ │ 対象期間   [2026/04/01] ～ [2026/04/30]                             │ │
│ │                                                                     │ │
│ │                                          [クリア]  [■ 検索 ■]       │ │
│ └───────────────────────────────────────────────────────────────────────┘ │
├───────────────────────────────────────────────────────────────────────────┤
│ 人事承認待ち: 3件                                                       │
├───────────────────────────────────────────────────────────────────────────┤
│ ┌──────────┬──────────┬────────┬──────────────────┬──────────────────┐   │
│ │ 申請者 ▼ │ 対象日 ▼ │修正種別│ 修正前→修正後    │ 修正理由         │   │
│ ├──────────┼──────────┼────────┼──────────────────┼──────────────────┤   │
│ │          │          │        │                  │                  │   │
│ ├──────────┼──────────┼────────┼──────────────────┼──────────────────┤   │
│ │          │          │        │                  │                  │   │
│ └──────────┴──────────┴────────┴──────────────────┴──────────────────┘   │
│                                                                         │
│ ┌──────────────────────┬──────────────────┐                             │
│ │ 上長承認者           │ 上長承認日 ▼     │ (テーブル続き)              │
│ ├──────────────────────┼──────────────────┤                             │
│ │ 鈴木部長             │ 04/01 14:00      │                             │
│ ├──────────────────────┼──────────────────┤                             │
│ │ 山田課長             │ 04/02 10:30      │                             │
│ └──────────────────────┴──────────────────┘                             │
├───────────────────────────────────────────────────────────────────────────┤
│ < 1 2 3 >                                             20件/ページ ▼   │
└───────────────────────────────────────────────────────────────────────────┘
```

**ワイヤーフレーム補足:**

テーブルは横幅の都合で2段に分割表示しているが、
実装上は1つのテーブルで全7カラムを横一列に並べる。

```
完全版テーブルヘッダー:
│ 申請者 ▼ │ 対象日 ▼ │修正種別│ 修正前→修正後 │ 修正理由       │上長承認者│上長承認日 ▼│

サンプル行:
│ 田中太郎 │ 03/15(土)│ 両方   │ 09:00 → 08:30 │ 出退勤両方の...│ 鈴木部長│ 04/01 14:00│
│ 佐藤花子 │ 03/20(木)│ 退勤   │ 17:00 → 18:30 │ ICカード不具...│ 山田課長│ 04/02 10:30│
│ 高橋美咲 │ 03/25(火)│ 出勤   │ --:-- → 09:00 │ 出勤打刻漏れ...│ 鈴木部長│ 04/03 09:15│
```

---

## コンポーネント構成

```
PendingHrClockCorrectionListPage
├── Breadcrumb
│   └── BreadcrumbItem ("ホーム", "申請管理", "人事承認待ち打刻修正一覧")
├── SearchForm (上部固定)
│   ├── DateRangePicker (対象期間)
│   └── ButtonGroup
│       ├── ClearButton (クリア)
│       └── SearchButton (検索)
├── ActionBar
│   └── ResultCount ("人事承認待ち: {N}件")
├── DataTable
│   ├── TableHeader (ソート可: 申請者, 対象日, 上長承認日)
│   ├── TableRow (クリックで詳細画面へ遷移)
│   │   ├── EmployeeNameCell (申請者名)
│   │   ├── TargetDateCell (対象日 + 曜日)
│   │   ├── CorrectionTypeCell (修正種別: 出勤/退勤/両方)
│   │   ├── ClockTimeDiffCell (修正前→修正後 時刻差分)
│   │   ├── ReasonCell (修正理由 ※省略表示)
│   │   ├── ApproverNameCell (上長承認者名)
│   │   └── DecidedAtCell (上長承認日時)
│   └── EmptyState ("人事承認待ちの打刻修正申請はありません")
└── Pagination
    ├── PageNumbers
    └── PageSizeSelector (20件/ページ)
```

---

## 表示項目

### 一覧カラム

| カラム | 表示名 | 型 | ソート | 幅 | 備考 |
|--------|--------|-----|--------|------|------|
| employeeName | 申請者 | text | ✅ | 120px | 氏名 |
| targetDate | 対象日 | date | ✅ (デフォルト昇順) | 120px | MM/DD(曜) 形式 |
| correctionType | 修正種別 | text | - | 80px | 出勤/退勤/両方 |
| clockTimeDiff | 修正前→修正後 | text | - | 180px | HH:mm → HH:mm 形式 |
| reason | 修正理由 | text | - | auto | 省略表示（30文字+...） |
| approverName | 上長承認者 | text | - | 100px | 上長承認者名 |
| decidedAt | 上長承認日 | datetime | ✅ | 100px | MM/DD HH:mm 形式 |

### 修正種別表示

| 値 | 表示 | 備考 |
|----|------|------|
| CLOCK_IN | 出勤 | 出勤打刻のみ修正 |
| CLOCK_OUT | 退勤 | 退勤打刻のみ修正 |
| BOTH | 両方 | 出勤・退勤両方を修正 |

### 修正前→修正後（clockTimeDiff）表示ルール

| 修正種別 | 表示形式 | 例 |
|---------|---------|-----|
| 出勤 | `{旧出勤} → {新出勤}` | `09:15 → 09:00` |
| 退勤 | `{旧退勤} → {新退勤}` | `17:00 → 18:30` |
| 両方 | `{旧出勤}/{旧退勤}` + 改行 + `→ {新出勤}/{新退勤}` | `09:30/17:00` → `09:00/18:00` |
| 打刻なし→追加 | `--:-- → {新時刻}` | `--:-- → 09:00` |

---

## 検索条件

| 条件 | フィールド名 | 入力形式 | 演算子 | デフォルト | 備考 |
|------|-------------|---------|--------|-----------|------|
| 対象期間 | targetDate | 日付範囲 (DateRange) | BETWEEN | 当月 (1日〜末日) | カレンダーピッカー |

> 人事承認待ち一覧は PENDING_HR 状態の申請のみに自動フィルタされる。
> 全件が仮締め後（afterProvisionalClose = true）の上長承認済み申請。
> ステータスフィルタや社員選択は不要（API側で制御）。

---

## アクション

### 画面アクション

| アクション | トリガー | 処理 | 権限 |
|-----------|---------|------|------|
| 検索 | 検索ボタン | 検索条件で一覧再取得 | read (人事) |
| クリア | クリアボタン | 検索条件をデフォルトに戻す | read |
| ソート変更 | カラムヘッダークリック | ソート条件変更 + 一覧再取得 | read |
| ページ切替 | ページネーション操作 | ページ変更 + 一覧再取得 | read |

### 行アクション

| アクション | 処理 | 備考 |
|-----------|------|------|
| 行クリック | 打刻修正申請詳細画面 (SCR-CC-002) へ遷移 | 詳細画面で人事承認を実行 |

---

## 状態管理

```typescript
/** 修正種別 */
type CorrectionType =
  | 'CLOCK_IN'
  | 'CLOCK_OUT'
  | 'BOTH';

/** ソート方向 */
type SortOrder = 'asc' | 'desc';

/** ソート可能カラム */
type SortableColumn =
  | 'employeeName'
  | 'targetDate'
  | 'decidedAt';

/** 人事承認待ち打刻修正申請アイテム */
interface PendingHrClockCorrectionItem {
  clockCorrectionRequestId: string;
  employeeId: string;
  employeeName: string;            // 従業員マスタから導出
  targetDate: string;              // ISO 8601 date
  correctionType: CorrectionType;
  originalClockIn: string | null;  // HH:mm
  originalClockOut: string | null; // HH:mm
  correctedClockIn: string | null; // HH:mm
  correctedClockOut: string | null;// HH:mm
  reason: string;
  afterProvisionalClose: boolean;  // 常に true
  approverId: string;
  approverName: string;            // 従業員マスタから導出
  decidedAt: string;               // ISO 8601 datetime (上長承認日時)
  requestedAt: string;             // ISO 8601 datetime
}

/** 検索パラメータ */
interface SearchParams {
  dateFrom: string;                // ISO 8601 date、デフォルト: 当月1日
  dateTo: string;                  // ISO 8601 date、デフォルト: 当月末日
}

/** ページネーション */
interface PaginationState {
  page: number;                    // 現在ページ (0-based)
  size: number;                    // 1ページあたり件数 (デフォルト: 20)
  sort: SortableColumn;            // ソートカラム (デフォルト: targetDate)
  order: SortOrder;                // ソート方向 (デフォルト: asc)
}

/** 画面全体の状態 */
interface PendingHrClockCorrectionListState {
  /** 検索条件 */
  searchParams: SearchParams;

  /** ページネーション */
  pagination: PaginationState;

  /** データ */
  data: {
    content: PendingHrClockCorrectionItem[];
    page: {
      number: number;
      size: number;
      totalElements: number;
      totalPages: number;
    };
    isLoading: boolean;
    error: {
      message: string;
      retryAction: (() => void) | null;
    } | null;
  };
}
```

---

## API連携

| 操作 | エンドポイント | メソッド | パラメータ |
|------|--------------|---------|-----------|
| 人事承認待ち一覧取得 | `/api/v1/clock-corrections/pending-hr` | GET | dateFrom, dateTo, page, size, sort |

### リクエスト例

```
GET /api/v1/clock-corrections/pending-hr
  ?dateFrom=2026-04-01
  &dateTo=2026-04-30
  &page=0
  &size=20
  &sort=targetDate,asc
```

### レスポンス例

```json
{
  "content": [
    {
      "clockCorrectionRequestId": "CCR-20260315-EMP003",
      "employeeId": "EMP-003",
      "employeeName": "田中太郎",
      "targetDate": "2026-03-15",
      "correctionType": "BOTH",
      "originalClockIn": "09:00",
      "originalClockOut": "17:00",
      "correctedClockIn": "08:30",
      "correctedClockOut": "18:00",
      "reason": "出退勤両方の打刻を修正します。ICカード不具合のため",
      "afterProvisionalClose": true,
      "approverId": "MGR-002",
      "approverName": "鈴木部長",
      "decidedAt": "2026-04-01T14:00:00+09:00",
      "requestedAt": "2026-04-01T09:00:00+09:00"
    }
  ],
  "page": {
    "number": 0,
    "size": 20,
    "totalElements": 1,
    "totalPages": 1
  }
}
```

---

## エラー処理

| エラー | HTTPステータス | 表示方法 | アクション |
|--------|--------------|---------|----------|
| 通信エラー | - | Toast (error) | 再試行ボタン |
| 認証エラー | 401 | リダイレクト | ログイン画面へ |
| 権限エラー | 403 | Toast (error) | 「人事担当者権限がありません」 |
| バリデーションエラー | 400 | Toast (warning) | 検索条件の修正を促す |
| サーバーエラー | 500 | Toast (error) | 再試行ボタン |

---

## レスポンシブ対応

| ブレークポイント | 対応 |
|----------------|------|
| Desktop (>1024px) | フル表示（全7カラム） |
| Tablet (768-1024px) | 修正理由・上長承認者カラム非表示（5カラム） |
| Mobile (<768px) | カード形式に切り替え（申請者+対象日+修正種別+上長承認者） |

### Mobile カード形式

```
┌──────────────────────────────────┐
│ 田中太郎                        │
│ 対象日 03/15(土)  両方          │
│ 09:00 → 08:30                   │
│ 上長承認: 鈴木部長 04/01 14:00  │
├──────────────────────────────────┤
│ 佐藤花子                        │
│ 対象日 03/20(木)  退勤          │
│ 17:00 → 18:30                   │
│ 上長承認: 山田課長 04/02 10:30  │
└──────────────────────────────────┘
```

---

## デザインガイドライン

> aesthetic: `clean-minimal` / colorScheme: `mono-accent`

### カラー＆テーマ

| 項目 | 値 | 備考 |
|------|-----|------|
| ベースカラー | #FFFFFF | ページ背景 |
| サーフェスカラー | #F9FAFB (gray-50) | 検索フォーム背景 |
| テキスト: Primary | #111827 (gray-900) | 見出し・データ値 |
| テキスト: Secondary | #6B7280 (gray-500) | ラベル・補足 |
| ボーダー | #E5E7EB (gray-200) | テーブル罫線・区切り |
| アクセントカラー | #2563EB (blue-600) | Primary ボタン・ソートアイコン |
| 件数ハイライト | #2563EB (blue-600) | 「人事承認待ち: 3件」の数字強調 |
| コントラスト比 | WCAG AA 準拠（4.5:1 以上） | テキスト/背景 |

### タイポグラフィ

| 要素 | フォント | サイズ | ウェイト | 備考 |
|------|---------|--------|---------|------|
| ページタイトル | system-ui | 24px | 700 | パンくず下 |
| 検索ラベル | system-ui | 12px | 500 | 検索条件ラベル |
| テーブルヘッダー | system-ui | 12px | 600 | カラム名 |
| テーブルデータ | system-ui | 14px | 400 | セルデータ |
| 時刻表示 | monospace | 14px | 400 | HH:mm 形式を等幅で |
| 矢印記号 | system-ui | 14px | 400 | → 記号 |
| 件数表示 | system-ui | 14px | 600 | 「人事承認待ち: 3件」 |
| ボタンラベル | system-ui | 14px | 500 | 検索 |

### レイアウト原則

| 項目 | 値 |
|------|-----|
| 基本余白 | 8px グリッド |
| ページ左右パディング | 24px |
| 検索フォーム: パディング | 16px |
| 検索フォーム: 角丸 | 8px |
| セクション間余白 | 16px |
| テーブル行高 | 48px (両方の場合は 64px) |
| ボタン角丸 | 6px |
| ボタン高さ | 36px |
| カード影 | なし（clean-minimal） |
| 区切り線 | 1px solid #E5E7EB |

### インタラクション

| 状態 | スタイル |
|------|---------|
| テーブル行: ホバー | 背景色 gray-50 + カーソル pointer |
| ソートヘッダー: ホバー | テキスト色 blue-600 + カーソル pointer |
| ソートヘッダー: アクティブ | ▲/▼ アイコン表示 + テキスト色 blue-600 |
| ボタン: ホバー | 背景色 5% 暗く |
| ボタン: disabled | opacity: 0.4 |
| フォーカス | 2px outline (blue-600) + 2px offset |
| ローディング | テーブル部分に Skeleton UI（5行分） |
| 画面遷移 | 即時（アニメーションなし） |
| フィードバック | Toast 通知（右上、3秒自動消去） |
| ページネーション: ホバー | 背景色 gray-100 |
| ページネーション: 現在ページ | 背景色 blue-600 + テキスト白 |

### アンチパターン（避けるべき表現）

- グラデーション背景の多用
- 装飾的なドロップシャドウの重ね掛け
- 無意味なアニメーション・トランジション
- ジェネリックなAI生成風のイラスト
- 過度な角丸（pill 形状の乱用）
- テーブルのゼブラストライプ（clean-minimalでは罫線のみ）
- 検索条件の過度な装飾（シンプルなフォームで十分）

---

## 品質チェック結果

- [x] 全クエリに対応する表示要素がある（7カラムのDataTable、1条件の検索フォーム、ページネーション）
- [x] 全コマンドに対応する操作UIがある（行クリック → 詳細画面遷移 → 人事承認アクション）
- [x] エラー状態の表示が定義されている（Toast通知 + 再試行ボタン、権限エラー対応）
- [x] 状態遷移がUI上で表現されている（PENDING_HR状態のみ表示、行クリックで詳細画面→人事承認へ）

<!-- メトリクスサマリー（/estimate 自動集計用）
画面種別: 一覧
コンポーネント数: 15
DataTable: 1
SearchForm: 1
フォーム数: 1
チャート数: 0
-->
